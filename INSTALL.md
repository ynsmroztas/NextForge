# Run

```bash
python3 -m pip install -r requirements.txt
python3 nextforge.py -t https://lab.example --full
```

If `nextforge.py` is the packed entry, drop the original operator next to it as `nextforge.py` from this repo release, or extract:

```bash
NEXTFORGE_EXTRACT=1 python3 nextforge.py --help
```
